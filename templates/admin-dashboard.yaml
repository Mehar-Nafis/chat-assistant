# Admin Dashboard — Deployment + Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "chatassistant.fullname" . }}-admin-dashboard
  labels:
    {{- include "chatassistant.labels" . | nindent 4 }}
    app.kubernetes.io/name: admin-dashboard
spec:
  replicas: {{ .Values.adminDashboard.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: admin-dashboard
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: admin-dashboard
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      securityContext:
        {{- include "chatassistant.securityContext" . | nindent 8 }}
      containers:
        - name: admin-dashboard
          image: {{ .Values.adminDashboard.image }}
          securityContext:
            {{- include "chatassistant.containerSecurityContext" . | nindent 12 }}
          env:
            - name: VITE_BACKEND_URL
              value: "http://{{ include "chatassistant.fullname" . }}-chain-server:{{ .Values.chainServer.port }}"
          ports:
            - containerPort: {{ .Values.adminDashboard.port }}
              name: http
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "chatassistant.fullname" . }}-admin-dashboard
  labels:
    {{- include "chatassistant.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.adminDashboard.servicePort }}
      targetPort: http
      name: http
  selector:
    app.kubernetes.io/name: admin-dashboard
    app.kubernetes.io/instance: {{ .Release.Name }}
